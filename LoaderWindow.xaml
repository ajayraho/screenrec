<Window x:Class="ScreenRecApp.LoaderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processing" Height="60" Width="200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True" ShowInTaskbar="False">
    <Border Background="#1e1e2e" CornerRadius="8" BorderBrush="#313244" BorderThickness="1">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressBar IsIndeterminate="True" Width="100" Height="4" Foreground="#89b4fa" Background="#181825" BorderThickness="0" Margin="0,0,10,0"/>
            <TextBlock Text="" Foreground="#a6adc8" FontSize="12" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
